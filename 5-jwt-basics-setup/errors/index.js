
const customErr = require('./custom-error')
const BadRequestErr = require('./bad-request')
const UnauthorisedErr = require('./unauthenticatedr')

module.exports = {
  customErr, BadRequestErr , UnauthorisedErr
}